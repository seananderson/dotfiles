[general]
ui = basic
#ui = Blinkenlights
accounts = seananderson
pythonfile=/Users/seananderson/.offlineimap/offlineimap.py
fsync = False

[Account seananderson]
localrepository = seananderson-local
remoterepository = seananderson-remote
status_backend = sqlite
#postsynchook = notmuch new
# Minutes between syncs
#autorefresh = 10
# Number of quick-syncs between autorefreshes. Quick-syncs do not update if the
# only changes were to IMAP flags
#quick = 10

[Repository seananderson-local]
type = Maildir
localfolders = ~/.mail/sean-seananderson.ca
nametrans = lambda folder: {'drafts': 'INBOX.Drafts',
                            'sent': 'INBOX.Sent Items',
                            'archive': 'INBOX.Archive',
                            'trash': 'INBOX.Trash',
                            'spam': 'INBOX.Junk Mail',
                            'inbox': 'INBOX',
														'toreply': 'INBOX.To Reply',
														'robots': 'INBOX.Robots'
														#'archive2004-2010': 'INBOX.Archive 2004-2010'
                            }.get(folder, folder)

[Repository seananderson-remote]
type = IMAP
remotehost = mail.messagingengine.com
remoteport = 993
maxconnections = 5
ssl = yes
cert_fingerprint=fa86c1ca2d4f805e8fe7b8e7076a3ad2ccc52bb4
remoteuser = seananderson@fastmail.fm
remotepasseval = get_keychain_pass(account="seananderson@fastmail.fm", server="mail.messagingengine.com")
realdelete = no
nametrans = lambda folder: {'INBOX.Drafts': 'drafts',
                            'INBOX.Sent Items': 'sent',
                            'INBOX.Archive': 'archive',
                            'INBOX.Trash': 'trash',
                            'INBOX.Junk Mail': 'spam',
                            'INBOX': 'inbox',
														'INBOX.To Reply': 'toreply',
														'INBOX.Robots': 'robots'
														#'INBOX.Archive 2004-2010': 'archive2004-2010'
                            }.get(folder, folder)

folderfilter = lambda folder: folder not in ['INBOX.Junk Mail',
                                             'INBOX.Archive 2004-2010'
                                             ]

# In between synchronisations, OfflineIMAP can monitor mailboxes for new
# messages using the IDLE command. If you want to enable this, specify here
# the folders you wish to monitor. Note that the IMAP protocol requires a
# separate connection for each folder monitored in this way, so setting
# this option will force settings for:
#     maxconnections - to be at least the number of folders you give
#     holdconnectionopen - to be true
#     keepalive - to be 29 minutes unless you specify otherwise
#
# This feature isn't complete and may well have problems. See the manual
# for more details.
#
# This option should return a Python list. For example
#
#idlefolders = ['INBOX']
